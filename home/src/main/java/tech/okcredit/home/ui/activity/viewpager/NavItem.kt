package tech.okcredit.home.ui.activity.viewpager

enum class NavItem(val id: Int) {
    // TODO (Add Label for Fragment names and give proper name to enum keys)
    HOME_FRAGMENT(0),
    HOME_PAYMENTS(1),
    DASHBOARD_FRAGMENT(2),
    HOME_HELP_SUPPORT_FRAGMENT(3),

    // TODO (Change Menu to Settings: Pratham)
    HOME_MENU_FRAGMENT(4)
}
