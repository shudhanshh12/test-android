package `in`.okcredit.onboarding.otp_verification

object OtpArgs {
    const val FLAG_DEFAULT = 0
    const val FLAG_AUTH_FAIL = 1

    @Deprecated(message = "should be moved to class where it belongs")
    const val ARG_FLAG = "flag"
}
